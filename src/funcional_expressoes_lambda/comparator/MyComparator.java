package funcional_expressoes_lambda.comparator;

import map.entities.Product;

public class MyComparator {
//public class MyComparator implements Comparator<Product> {

	
	// Comparando dos produtos com Comparator
	// Um número Inteiro caso seja positivo que dizer que o primeiro é maior que o segundo.
	// Se o número for (0) que dizer que os dois prosutos são iguais. 
	// Se for negativo é que o primeiro é menor que o segundo produtor.

	//@Override
	//public int compare(Product p1, Product p2) {
	//	return p1.getName().toUpperCase().compareTo(p2.getName().toUpperCase());
	//}
}
